$(document).ready(function(){	updateTotal();})$('.addToCart').on("click", function() {	let id = $(this).attr('id');	let price = $(this).data('index');	// alert(price);		$.ajax({		url: 'controllers/add_to_cart.php',		method: 'POST',		data: {id:id, price:price},	}).done(data => {		// alert(data);	})})$('input#quan').on("change", function () {	let quantity= $(this).val();	let price = $(this).data('index');	let subtotal= quantity * price;	console.log(quantity);	$(this).next('p#side_price').text(subtotal);	updateTotal();		let id = $(this).attr('data-id');	let test =$('table.table').find('tbody').find('tr').find('td').find('.input-' + id).val(quantity);// 	console.log(test)	})function updateTotal() {	let subtotal=0;$('p#side_price').each(function() {	subtotal += parseInt($(this).text());	// console.log(subtotal);	// let total=0;	// total = total + parseFloat(subtotal);	// alert(total)	$('span#totalamt').text(subtotal);})}$('input#quan').on("change", function() {	$('input#quan').each(function() {		let value = $(this).val();		let id = $(this).data('id');		// alert(id);		$.ajax({			url:'controllers/cart_quantity.php',			method: 'post',			data: {value:value, id:id},		}).done(data => {			// alert(data);		})	})})$('button.btn.btn-danger.addToCart').click(function() {	location.reload();})// function summary() {// 	let subtotal=0;// $('td#price').each(function() {// 	subtotal += parseInt($(this).text());// 	console.log(subtotal);// 	// let total=0;// 	// total = total + parseFloat(subtotal);// 	// alert(total)// 	$('span#summary_subtotal').text(subtotal);// })// }// $('button#update').on("click", function () {// 	let id = $(this).val()// 	let input = $(this).parents('td').find('input#input').val();// 	$.ajax({// 		url: 'controllers/updateInput.php',// 		method: 'POST',// 		data: {id:id, input:input},// 	}).done(data => {// 		console.log(data)// 	})// })// $('span#totalamount').on("change", function() {// 	let total=0;// 	let subtotal= $(this).text()// 	// console.log(subtotal);// })