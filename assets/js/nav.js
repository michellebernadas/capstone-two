$(document).ready(function(){    $(".dropdown").hover(                    function() {            $('.dropdown-menu', this).not('.in .dropdown-menu').stop(true,true).slideDown("fast");            $(this).toggleClass('open');                },        function() {            $('.dropdown-menu', this).not('.in .dropdown-menu').stop(true,true).slideUp("fast");            $(this).toggleClass('open');               }    );    $('img.cart').on('click', function() {    $('#navbarSide').addClass('reveal');    $('.overlay').show();  });  $('.overlay').on('click', function(){    $('#navbarSide').removeClass('reveal');    $('.overlay').hide();  });});  $('button#signup').click( () => {    let username =  $('input#username2').val();      let email = $('input#email').val();                let errorFlag= false;                // let sheer=false;                $.ajax({                    // async: false,                    url: 'controllers/check_username.php',                    method: 'POST',                    data:{username:username},                }).done(data => {                    // console.log(data);                    if(data=='found') {                        $('#username2').next().css('color', 'red');                        $('#username2').next().html('Username Already Exists');                         // $('input#email').next().css('color', 'red');                        // $('input#email').next().html('Email Already Exists');                        errorFlag=true;                    } else {                        $('#username2').next().css('color', 'green');                        $('#username2').next().html('Username still available');                        // $('input#email').next().css('color', 'green');                        // $('input#email').next().html('Email still available');                    }                     if (username.length == 0 ) {                          $('#username2').next().css('color', 'red');                          $('#username2').next().html('This field is required');                          errorFlag= true;                    }                    if (email.length == 0) {                        $('input#email').next().css('color', 'red');                        $('input#email').next().html('This field is required');                    }else {                        $('input#email').next().html('');                    }                    })                    const password = $('#password2').val();                    const confirmPassword = $('#confirmPassword').val();                    if (password.length == 0 ) {                        $('#password2').next().css('color', 'red');                        $('#password2').next().html('This field is required');                        errorFlag= true;                    } else {                        $('#password2').next().html('');                        if (password.length < 8) {                            $('#password2').next().css('color', 'red');                            $('#password2').next().html('Minimum of Eight Characters');                            errorFlag=true;                        }                        if (password !== confirmPassword) {                            $('#confirmPassword').next().css('color', 'red');                            $('#confirmPassword').next().html('Passwords did not match');                            errorFlag = true;                        } else {                            $('#confirmPassword').next().html('');                        }                    }                    if (errorFlag == false) {                        $('#registerForm').submit();                        $('regModal').show();                    }})$('button#login').click( function() {    let username = $('input#username').val();    let password = $('input#password').val();    // console.log(password);    errorFlag= false;   if (username.length == 0 ) {      $('#username2').next().css('color', 'red');      $('#username2').next().html('This field is required');      // errorFlag= true;    }    if (password.length == 0 ) {            $('#password2').next().css('color', 'red');            $('#password2').next().html('This field is required');            errorFlag= true;    }})$(window).scroll(function(){    // let pos = jQuery(this).scrollTop();        // console.log(pos);    if ($(window).scrollTop() >= 100 ) {        $('#master').addClass('sticky');        $('img#down').show();        // $('nav div').addClass('visible-title');    }    else {        $('#master').removeClass('sticky');        $('img#down').hide();        $('img#up').hide();        // $('nav div').removeClass('visible-title');    }    if (($(window).scrollTop() >= 4400 )) {        // $('img#down').addClass('rotate')        $('img#down').hide();        $('img#up').show();    }});// $('.sideremove').click(function() {//     let button = $(this)//     let id = $(this).data('id');//     let card =$(this).parents().find('.card.side').data('id')//     // let price =$(this).data('price')//     console.log(card);//     $.ajax({//         type:'DELETE',//         url: 'controllers/navremove.php',//         method: 'POST',//         data: {id:id},//     }).done(data => {//         // card.remove();//         // alert(data);//     })// })  